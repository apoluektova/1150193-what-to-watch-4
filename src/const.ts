export const ALL_GENRES = `All genres`;

export const SIMILAR_MOVIES_COUNT = 4;

export enum Tabs {
  OVERVIEW = `Overview`,
  DETAILS = `Details`,
  REVIEWS = `Reviews`,
};

export enum Review {
  MIN_LENGTH = 50,
  MAX_LENGTH = 400,
};

export enum AppRoute {
  MAIN = `/`,
  SIGN_IN = `/login`,
  MY_LIST = `/mylist`,
  MOVIE = `/films`,
  PLAYER = `/player`,
};
